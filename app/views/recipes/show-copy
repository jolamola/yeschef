<%= render "partials/navbar" %>
<div class = "center-width">

	<div>

		<!-- sub menu for recipes -->
		<h1> <strong><%= @recipe.rName %></strong> </h1>
		<%= link_to "Back", recipes_path %> | 
		<%= link_to "Edit Info", edit_recipe_path %> | 
		<%= link_to "Add Component", new_recipe_component_path(@recipe) %> | 
		<%= link_to "Delete Recipe", recipe_path, method: :delete, data: {Confirm: "Your recipe will be permanently deleted"} %>
		<br>
		<br>
	</div>


	<hr>

	<div class = "row">
	<!-- flash confirmation of recipe add -->
		<div>
			<% flash.each do |key, value| %>
			<div class="alert alert-<%= key %>" role="alert-dismissible">
				<button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
				<strong><%= value %></strong>
			</div>
			<% end %>
		</div>

		<!-- displays recipe details on show -->
		<div class = "col-md-5">
			<br>
			<tr>
				<td><%= image_tag @recipe.photo.url(:med), alt: "no image uploaded :(" %></td>
				<br><br>
				<td><strong>Description: </strong><%= @recipe.serving_size %></td>
				<br><br>
				<td><strong>Serving size: </strong><%= @recipe.ingredient %></td>
				<br><br>
				<td><strong>Suggested Equipment: </strong><%= @recipe.instruction %></td><br>
			</tr>
		</div>

		<!-- displays components of recipe -->
		<div class = "col-md-7">
			<h3> Components List:</h3>
			<% @recipe.components.each do |c|%>
			<div>
				<div>
					<strong>Component: </strong><%= c.cName %>
					<br>
					<%= link_to "Edit", edit_recipe_component_path(c.recipe, c) %> | <%= link_to "Delete", recipe_component_path(c.recipe.id, c.id), method: :delete, data: {Confirm: "Your component will be permanently deleted"} %>
				</div>				

				<ul>
					Ingredients:
					<ul><%= simple_format c.ingredient %></ul>
				</ul>
				<ul>
					Directions:
					<ul><%= simple_format c.direction %></ul>
				</ul>

				<br>
			</div>
			<% end %>
		</div>
	</div>
</div>





